# FTC 队伍网站维护指南

## 目录
1. [文件结构说明](#文件结构说明)
2. [日常内容更新](#日常内容更新)
3. [编辑工具设置](#编辑工具设置)
4. [本地测试方法](#本地测试方法)
5. [网站部署上线](#网站部署上线)
6. [常见维护任务](#常见维护任务)
7. [故障排查](#故障排查)

---

## 文件结构说明

```
ftc-website/
├── index.html          # 首页
├── about.html          # 关于我们
├── seasons.html        # 赛季档案（总览）
├── outreach.html       # 外联活动
├── resources.html      # 资源分享
├── sponsors.html       # 赞助商
├── style.css           # 全局样式文件（重要！）
├── seasons/            # 赛季详情页面文件夹
│   ├── 2026.html
│   ├── 2025.html
│   └── ... (2024-2018)
└── images/             # 图片文件夹
    ├── robots/         # 机器人照片
    ├── logos/          # 赛季 LOGO
    ├── sponsors/       # 赞助商 LOGO
    └── outreach/       # 外联活动照片
```

---

## 日常内容更新

### 1. 添加机器人照片

**步骤：**
1. 准备两种尺寸的照片：
   - **缩略图**：建议 400x300 像素（用于赛季档案页面）
   - **大图**：建议 1200x800 像素（用于赛季详情页面）

2. 命名规范：
   ```
   缩略图：2026-robot-thumb.jpg
   大图：  2026-robot-main.jpg
   ```

3. 将照片放入 `/images/robots/` 文件夹

4. 照片会自动显示，无需修改代码

**图片优化建议：**
- 使用 [TinyPNG](https://tinypng.com/) 压缩图片
- 推荐格式：JPG（照片）、PNG（LOGO）
- 单张图片建议不超过 500KB

### 2. 添加 FTC 赛季 LOGO

**步骤：**
1. 从 FTC 官网下载赛季 LOGO
2. 命名格式：`2026-logo.png`
3. 放入 `/images/logos/` 文件夹
4. LOGO 会自动显示在赛季卡片右上角

**LOGO 要求：**
- 推荐尺寸：200x200 像素
- 格式：PNG（支持透明背景）
- 背景：透明或纯色

### 3. 修改文字内容

**基本步骤：**
1. 用文本编辑器打开对应的 HTML 文件
2. 找到需要修改的文字（使用 Ctrl+F / Cmd+F 搜索）
3. 直接替换文字内容
4. 保存文件（Ctrl+S / Cmd+S）
5. 刷新浏览器查看效果

**示例：修改机器人名称**
```html
<!-- 在 seasons/2026.html 中找到： -->
<h1>机器人名称</h1>

<!-- 改为： -->
<h1>Artemis III</h1>
```

### 4. 更新团队成员信息

**位置：** `about.html`

**步骤：**
1. 打开 `about.html`
2. 找到团队成员部分（搜索 "team-member"）
3. 修改成员姓名和职位：
```html
<div class="team-member">
    <h3>张三</h3>
    <p>队长 / 编程</p>
</div>
```

### 5. 添加赞助商

**步骤：**
1. 将赞助商 LOGO 放入 `/images/sponsors/`
2. 打开 `sponsors.html`
3. 找到赞助商卡片部分
4. 复制一个现有的卡片代码，修改内容：
```html
<div class="sponsor-card">
    <img src="images/sponsors/公司名.png" alt="公司名">
    <h3>公司名称</h3>
    <p>赞助级别：金牌赞助商</p>
</div>
```

---

## 编辑工具设置

### 推荐工具

**初学者：**
- **Windows**: Notepad++（免费）
  - 下载：https://notepad-plus-plus.org/
- **Mac**: TextEdit（系统自带）
  - 注意：需要切换到"纯文本"模式（格式 → 制作纯文本）

**进阶用户（强烈推荐）：**
- **VS Code**（免费，跨平台）
  - 下载：https://code.visualstudio.com/
  - 功能强大，支持语法高亮、自动补全

### VS Code 设置（推荐）

**安装步骤：**
1. 下载并安装 VS Code
2. 打开 VS Code
3. 点击左侧"扩展"图标（或按 Ctrl+Shift+X / Cmd+Shift+X）
4. 搜索并安装以下插件：
   - **Live Server**（实时预览）
   - **HTML CSS Support**（代码提示）
   - **Prettier**（代码格式化）

**使用方法：**
1. 在 VS Code 中打开整个 `ftc-website` 文件夹
2. 右键点击 `index.html`
3. 选择 "Open with Live Server"
4. 浏览器会自动打开网站
5. 修改代码后，浏览器会自动刷新

---

## 本地测试方法

### 方法一：直接打开（最简单）

1. 找到 `index.html` 文件
2. 双击文件，用浏览器打开
3. 修改代码后，刷新浏览器（F5）

**优点：** 简单快速
**缺点：** 需要手动刷新

### 方法二：使用 Live Server（推荐）

1. 在 VS Code 中安装 Live Server 插件
2. 右键点击 `index.html` → "Open with Live Server"
3. 修改代码后自动刷新

**优点：** 自动刷新，开发效率高
**缺点：** 需要安装 VS Code

### 方法三：使用 Python 本地服务器

如果你的电脑安装了 Python：

```bash
# 在网站文件夹中打开终端，运行：
python -m http.server 8000

# 然后在浏览器打开：
http://localhost:8000
```

---

## 网站部署上线

### 选项一：GitHub Pages（推荐，免费）

**优点：**
- 完全免费
- 自动部署
- 支持自定义域名
- 稳定可靠

**步骤：**

1. **创建 GitHub 账号**
   - 访问 https://github.com/
   - 点击 "Sign up" 注册

2. **创建新仓库**
   - 登录后点击右上角 "+" → "New repository"
   - 仓库名：`ftc-website`（或任意名称）
   - 设置为 Public（公开）
   - 点击 "Create repository"

3. **上传文件**

   **方法 A：使用 GitHub 网页界面（简单）**
   - 在仓库页面点击 "uploading an existing file"
   - 拖拽所有网站文件到页面
   - 点击 "Commit changes"

   **方法 B：使用 Git 命令行（推荐）**
   ```bash
   # 在网站文件夹中打开终端
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/你的用户名/ftc-website.git
   git push -u origin main
   ```

4. **启用 GitHub Pages**
   - 进入仓库的 "Settings"
   - 左侧菜单找到 "Pages"
   - Source 选择 "main" 分支
   - 点击 "Save"
   - 等待 1-2 分钟，会显示网站地址：
     `https://你的用户名.github.io/ftc-website/`

5. **更新网站**
   - 修改本地文件后
   - 重新上传到 GitHub（或使用 git push）
   - 等待 1-2 分钟自动部署

### 选项二：Netlify（简单快速）

**优点：**
- 拖拽上传，极其简单
- 自动 HTTPS
- 免费额度充足

**步骤：**
1. 访问 https://www.netlify.com/
2. 注册账号（可用 GitHub 登录）
3. 点击 "Add new site" → "Deploy manually"
4. 将整个 `ftc-website` 文件夹拖拽到页面
5. 等待部署完成，获得网址

**更新网站：**
- 直接拖拽新文件到 Netlify 即可

### 选项三：Vercel

**步骤：**
1. 访问 https://vercel.com/
2. 注册账号
3. 导入 GitHub 仓库或直接上传文件
4. 自动部署

---

## 常见维护任务

### 任务 1：更新赛季信息

**场景：** 新赛季开始，需要更新机器人信息

**步骤：**
1. 打开 `/seasons/2026.html`（或对应年份）
2. 修改以下内容：
   - 机器人名称（`<h1>` 标签）
   - 机器人介绍（`<p>` 标签）
   - 技术规格（设计卡片中）
   - 赛季成就（成就列表）
3. 添加机器人照片到 `/images/robots/`
4. 保存并测试

### 任务 2：添加新赛季

**场景：** 需要为新赛季创建页面

**步骤：**
1. 复制 `/seasons/2026.html`
2. 重命名为新赛季年份（如 `2027.html`）
3. 打开文件，修改所有年份相关内容：
   - 页面标题
   - 赛季名称
   - 图片路径
4. 打开 `seasons.html`
5. 在赛季卡片区域添加新卡片：
```html
<div class="season-card">
    <img src="images/logos/2027-logo.png" alt="2027 赛季 LOGO" class="season-logo">
    <img src="images/robots/2027-robot-thumb.jpg" alt="2027 机器人">
    <h3>2026-2027 赛季</h3>
    <p class="season-name">INTO THE DEEP</p>
    <a href="seasons/2027.html" class="btn">查看详情</a>
</div>
```

### 任务 3：添加外联活动

**位置：** `outreach.html`

**步骤：**
1. 打开 `outreach.html`
2. 找到活动卡片部分
3. 复制一个现有卡片，修改内容：
```html
<div class="activity-card">
    <h3>活动名称</h3>
    <p class="activity-date">2026年3月15日</p>
    <p>活动描述...</p>
</div>
```
4. 添加活动照片到 `/images/outreach/`

### 任务 4：更新资源文件

**位置：** `resources.html`

**步骤：**
1. 将资源文件（代码、CAD、文档）放入对应文件夹
2. 打开 `resources.html`
3. 找到对应的资源卡片
4. 更新下载链接：
```html
<a href="downloads/code/autonomous.java" class="btn" download>下载代码</a>
```

### 任务 5：修改配色方案

**位置：** `style.css`

**步骤：**
1. 打开 `style.css`
2. 找到 `:root` 部分（文件开头）
3. 修改颜色变量：
```css
:root {
    --primary: #FF0000;      /* 主色（红色）*/
    --secondary: #CC0000;    /* 次要色（深红）*/
    --accent: #999999;       /* 强调色（灰色）*/
    --bg-dark: #0a0a0a;      /* 背景色（黑色）*/
}
```
4. 保存后，整个网站配色会自动更新

---

## 故障排查

### 问题 1：页面显示错误或布局混乱

**可能原因：**
- HTML 标签没有正确闭合
- 不小心删除了重要代码

**解决方法：**
1. 检查是否有未闭合的标签（如 `<div>` 没有对应的 `</div>`）
2. 使用浏览器的"查看源代码"功能检查
3. 在 VS Code 中，HTML 标签会自动配对高亮
4. 如果无法修复，从备份恢复文件

### 问题 2：样式丢失，页面变成纯文本

**可能原因：**
- `style.css` 文件被删除或移动
- CSS 文件链接路径错误

**解决方法：**
1. 确认 `style.css` 文件存在于网站根目录
2. 检查 HTML 文件中的 CSS 链接：
```html
<link rel="stylesheet" href="style.css">
```
3. 如果是子文件夹中的页面（如 `seasons/2026.html`），路径应该是：
```html
<link rel="stylesheet" href="../style.css">
```

### 问题 3：图片不显示

**可能原因：**
- 图片路径错误
- 文件名大小写不匹配
- 图片格式不支持

**解决方法：**
1. 检查图片文件是否存在于指定路径
2. 确认文件名完全一致（包括大小写）
   - 错误：`Robot.jpg` vs `robot.jpg`
3. 检查图片路径：
   - 根目录页面：`images/robots/2026-robot-main.jpg`
   - 子文件夹页面：`../images/robots/2026-robot-main.jpg`
4. 支持的格式：JPG、PNG、GIF、WebP

### 问题 4：链接点击后显示 404

**可能原因：**
- 链接的文件不存在
- 文件路径错误

**解决方法：**
1. 检查链接指向的文件是否存在
2. 确认路径正确：
```html
<!-- 从 index.html 链接到赛季页面 -->
<a href="seasons/2026.html">查看详情</a>

<!-- 从 seasons/2026.html 返回首页 -->
<a href="../index.html">返回首页</a>
```

### 问题 5：修改后浏览器没有更新

**可能原因：**
- 浏览器缓存

**解决方法：**
1. 强制刷新：
   - Windows: Ctrl + F5
   - Mac: Cmd + Shift + R
2. 清除浏览器缓存
3. 使用浏览器的"无痕模式"测试

### 问题 6：GitHub Pages 部署后样式错误

**可能原因：**
- 文件路径在本地和线上不一致

**解决方法：**
1. 检查所有资源路径是否使用相对路径
2. 确保没有使用绝对路径（如 `/Users/...`）
3. CSS 和图片路径应该是：
```html
<link rel="stylesheet" href="style.css">
<img src="images/robots/2026-robot-main.jpg">
```

---

## 维护最佳实践

### 1. 定期备份

**建议：**
- 每次重大修改前备份整个文件夹
- 使用 Git 进行版本控制
- 定期将文件上传到云盘（Google Drive、OneDrive 等）

**备份方法：**
```bash
# 复制整个文件夹
cp -r ftc-website ftc-website-backup-2026-02-24
```

### 2. 文件命名规范

**规则：**
- 使用英文和数字
- 不要使用空格（用 `-` 或 `_` 代替）
- 保持小写（避免大小写问题）
- 有意义的名称

**示例：**
- ✅ `2026-robot-main.jpg`
- ✅ `team-photo-2026.jpg`
- ❌ `机器人照片.jpg`
- ❌ `IMG_1234.jpg`
- ❌ `Robot Photo 2026.jpg`

### 3. 图片优化

**建议：**
- 上传前压缩图片（使用 TinyPNG）
- 选择合适的格式：
  - 照片：JPG
  - LOGO/图标：PNG
  - 动画：GIF
- 控制文件大小：
  - 缩略图：< 100KB
  - 大图：< 500KB

### 4. 代码格式化

**建议：**
- 保持代码缩进一致
- 使用 Prettier 插件自动格式化
- 添加注释说明重要部分

### 5. 测试清单

**每次修改后检查：**
- [ ] 所有页面都能正常打开
- [ ] 导航链接都能正常跳转
- [ ] 图片都能正常显示
- [ ] 在不同浏览器测试（Chrome、Safari、Firefox）
- [ ] 在手机上测试响应式布局

---

## 快速参考

### 常用文件位置

| 内容 | 文件位置 |
|------|----------|
| 首页内容 | `index.html` |
| 团队信息 | `about.html` |
| 赛季总览 | `seasons.html` |
| 赛季详情 | `seasons/2026.html` |
| 外联活动 | `outreach.html` |
| 资源分享 | `resources.html` |
| 赞助商 | `sponsors.html` |
| 全局样式 | `style.css` |
| 机器人照片 | `images/robots/` |
| 赛季 LOGO | `images/logos/` |

### 常用代码片段

**添加按钮：**
```html
<a href="链接地址" class="btn">按钮文字</a>
```

**添加卡片：**
```html
<div class="card">
    <h3>标题</h3>
    <p>内容</p>
</div>
```

**添加图片：**
```html
<img src="images/文件名.jpg" alt="图片描述">
```

---

## 获取帮助

**遇到问题时：**
1. 查看本维护指南的"故障排查"部分
2. 使用浏览器的"开发者工具"（F12）查看错误信息
3. 搜索错误信息（Google、Stack Overflow）
4. 咨询有经验的队员或导师

**学习资源：**
- HTML 教程：https://www.w3schools.com/html/
- CSS 教程：https://www.w3schools.com/css/
- MDN Web 文档：https://developer.mozilla.org/zh-CN/

---

**文档版本：** 1.0
**最后更新：** 2026-02-24
**维护者：** FTC 队伍技术组

---

## 附录：Git 版本控制（进阶）

如果你想使用 Git 进行版本控制：

### 初始化 Git 仓库

```bash
cd ftc-website
git init
git add .
git commit -m "Initial commit"
```

### 日常使用

```bash
# 查看修改状态
git status

# 添加修改的文件
git add .

# 提交修改
git commit -m "更新 2026 赛季信息"

# 推送到 GitHub
git push origin main
```

### 查看历史

```bash
# 查看提交历史
git log

# 查看具体修改
git diff
```

### 回退版本

```bash
# 查看历史提交
git log --oneline

# 回退到指定版本
git checkout <commit-id>
```

---

**祝你的 FTC 队伍网站运营顺利！** 🚀
